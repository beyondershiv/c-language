#include<stdio.h>
int main()
{
    int num;
    printf("enter number to find if it is even or odd");
    scanf("%d",&num);
    if(num%2==0)
    {
        printf("the number %d is a even number",num);
    }
    else 
    {
        printf("the given number %d is odd",num);
    }
}
