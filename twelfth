#include<stdio.h>
int main()
{
    int i,n,sum=0;
    printf("write the number till you want sum of");
    scanf("%d",&n);
    for(i;i<=n;i++)
    {
        sum=sum+i;
    }
    printf("sum=%d",sum);
    return 0;
}
